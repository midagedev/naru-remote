<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  App Store / TestFlight export options for `scripts/testflight-upload.sh`.

  Nothing secret lives here: the team ID is already in `project.yml`, and the
  API key and issuer stay in ~/.appstoreconnect (never in the repo).

  `manageAppVersionAndBuildNumber` is false on purpose — the build number is
  `CURRENT_PROJECT_VERSION` in `project.yml`, so it stays reviewable in git
  instead of being silently rewritten at export time.
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store-connect</string>
    <key>teamID</key>
    <string>XEF9KH7N43</string>
    <key>uploadSymbols</key>
    <true/>
    <key>manageAppVersionAndBuildNumber</key>
    <false/>
    <key>destination</key>
    <string>export</string>
</dict>
</plist>
