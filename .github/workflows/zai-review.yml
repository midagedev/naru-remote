name: Hermes PR Review

on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize]
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: hermes-review-${{ github.event.pull_request.number || inputs.pr_number || github.run_id }}
  cancel-in-progress: true

jobs:
  review:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (
        github.event_name == 'pull_request' &&
        github.event.pull_request.draft == false &&
        github.event.pull_request.head.repo.full_name == github.repository
      )
    uses: midagedev/review-actions/.github/workflows/hermes-pr-review.yml@v2
    with:
      pr_number: ${{ github.event.pull_request.number || inputs.pr_number }}
      prompt_path: .github/zai-review-prompt.md
      runner_labels: '["self-hosted","vps-hermes","naru-remote"]'
      approve_when_clean: true
      context_paths: |
        CLAUDE.md
        AGENTS.md
        .specify/memory/constitution.md
        .specify/feature.json
        specs/001-naru-remote-mvp/spec.md
        specs/001-naru-remote-mvp/plan.md
        specs/001-naru-remote-mvp/tasks.md
        specs/002-direct-keystroke-mode/spec.md
        specs/003-session-experience/spec.md
        specs/004-rfb-encodings/spec.md
